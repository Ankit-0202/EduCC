/**
 * @file AST.cpp
 * @brief Implementation file for AST classes in EduCC.
 *
 * Currently, most methods are inline in the header, so we leave this empty
 * or use it for expansions in the future (e.g. AST transformation passes).
 */

#include "ast/AST.h"

// Nothing here yet. We can add out-of-line definitions if needed.

